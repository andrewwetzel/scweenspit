<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ScweenSpit.Launcher</RootNamespace>
    <!-- This IS ScweenSpit as far as anyone downloading it is concerned. -->
    <AssemblyName>ScweenSpit</AssemblyName>
    <!-- Overridden by CI from the release tag; 0.0.0 locally so a dev build never looks current. -->
    <Version>0.0.0</Version>
    <!-- LibraryImport source generation emits unsafe marshalling code -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

    <!-- Native AOT: this has to start on a machine with no .NET at all, which is the whole point.
         It also keeps the download small. AOT cannot be cross-compiled, so only CI (windows-latest)
         can publish it; plain `dotnet build` still typechecks anywhere. -->
    <PublishAot>true</PublishAot>
    <IlcOptimizationPreference>Size</IlcOptimizationPreference>
    <IlcFoldIdenticalMethodBodies>true</IlcFoldIdenticalMethodBodies>

    <InvariantGlobalization>true</InvariantGlobalization>
    <StackTraceSupport>false</StackTraceSupport>
    <EventSourceSupport>false</EventSourceSupport>
    <MetadataUpdaterSupport>false</MetadataUpdaterSupport>
    <HttpActivityPropagationSupport>false</HttpActivityPropagationSupport>

    <!-- Left on: stripping exception messages turns the launcher's only error channel into
         resource keys like "UnauthorizedAccess_IODenied_NoPathName", which tells a user nothing. -->
    <UseSystemResourceKeys>false</UseSystemResourceKeys>
  </PropertyGroup>

  <ItemGroup>
    <!-- The real app, embedded. Absent on a bare checkout; CI publishes it here first. -->
    <EmbeddedResource Include="payload/ScweenSpit.exe" LogicalName="ScweenSpit.exe"
                      Condition="Exists('payload/ScweenSpit.exe')" />
  </ItemGroup>

</Project>
